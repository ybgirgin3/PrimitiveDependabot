import { User, Repo } from '../interfaces/RequestBody';

export const controller = (user: User[] | User, repo: Repo[] | Repo) => {
  // dosyayi fetch et
  // console.log(user);
  // console.log(repo);
  // save datas if there is no in
};
